key questions:
1. Yes use rich for fancy displays
2. 1 hour is fine
3. real-time live updating: real time is good
4. simplest version first

So now take me through what i need to do in phase2. give me hints, but do not give me the actual code, I would like to get better at programming still ;)

Edit
## Phase 2: Core Python Infrastructure (Guided Hints)

Let's break down Phase 2 into clear steps. You'll get hints and tips, but you'll write the code yourself!

---

### Step 1: Directory Structure

**Task:**  
Create the following directories inside `~/.ai_cli/`:

- `usage_monitor/`
- `usage_monitor/models/`
- `usage_monitor/core/`
- `usage_monitor/cli/`

**Hint:**  
Each directory should have an empty `__init__.py` file to make it a Python package.

**Test:**  
From `~/.ai_cli`, run:
```bash
python3 -c "import usage_monitor; print('Success!')"
```

---

### Step 2: Data Models

**Task:**  
Create `~/.ai_cli/usage_monitor/models/data_structures.py` with two classes.

#### UsageEntry Class

- Match the fields in your `usage.jsonl` (e.g., `timestamp`, `model`, `input_tokens`, `output_tokens`, `cost_usd`, `response_time_ms`, `request_id`)
- Add a method to convert from a dictionary (for reading JSON)
- Add a property to calculate `total_tokens`

we create individual UsageEntry objects, one per API call

#### Session Class

So this collects all the usageEntry objects we have had for the session, let's say last 4 hours, and we show the tokens and costs.

- Represents a group of API calls
- Fields: `id`, `start_time`, `end_time`, list of entries, `total_cost`, `models_used`
- Methods: add an entry, calculate duration in minutes
- Property: total token count from all entries


**Hints:**

- Use `@dataclass` for easier class definitions
- Use `datetime` for timestamps
- Use `List[UsageEntry]` for type hints

**Test:**  
Try creating objects and converting between JSON and Python objects.

---

### Step 3: Usage Reader

**Task:**  
Create `~/.ai_cli/usage_monitor/core/usage_reader.py` to read usage data.

**Hints:**

- Read `usage.jsonl` line by line
- Parse each line with `json.loads()`
- Use `try/except` for malformed JSON
- Return a list of `UsageEntry` objects, sorted by timestamp

**Suggested function signature:**
```python
def read_usage_entries(data_dir: str = None) -> List[UsageEntry]:
    # Your code here
    pass
```

**Test:**  
Generate some usage data with your bash script, then test your reader.

---

### Step 4: Simple CLI Test

**Task:**  
Create `~/.ai_cli/usage_monitor/cli/commands.py` with a test function.

**Hints:**

- Load recent entries using your usage reader
- Print number of entries, total tokens, total cost

**Suggested function:**
```python
def show_current_usage():
    entries = read_usage_entries()
    print(f"Found {len(entries)} API calls")
    # Calculate and print totals
```

**Test:**  
From `~/.ai_cli`, run:
```bash
python3 -c "from usage_monitor.cli.commands import show_current_usage; show_current_usage()"
```

---

### Step 5: Bash Script Integration

**Task:**  
Modify your bash script to call your Python function.

**Hint:**  
Example:
```bash
"burn")
    cd "$CONTEXT_DIR"
    python3 -c "from usage_monitor.cli.commands import show_current_usage; show_current_usage()"
    ;;
```

**Test:**  
Run `ai burn` and check if usage data is displayed.

---

### Debugging Tips

- **Import errors?** Check for `__init__.py` files.
- **Module not found?** Make sure you're in the `~/.ai_cli` directory.
- **JSON errors?** Check your `usage.jsonl` format.
- **No data?** Ensure your bash script is generating entries.

---

### Questions for You

- Do you want to complete all of Step 1 first, or go step-by-step?
- Are you comfortable with Python classes and dataclasses?
- Any specific areas where you want more detailed hints?

**Start with Step 1 (directory structure) and let me know when you're ready for the next step!**
